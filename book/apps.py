"""
This module defines the BookConfig class, which is responsible for configuring the Book app.
"""

from django.apps import AppConfig

class BookConfig(AppConfig):
    """
    AppConfig for the Book app.
    """
    name = 'book'
